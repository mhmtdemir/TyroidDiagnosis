<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/tw__blue_default</item>
        <item name="colorPrimaryDark">@color/tw__blue_default</item>
        <item name="colorAccent">@color/tw__blue_default</item>
    </style>

    <style name="StepperLayoutTheme" parent="MSDefaultStepperLayoutTheme"></style>

    <style name="DialogSlideAnimation">

        <item name="android:windowEnterAnimation">@anim/push_left_in</item>
        <item name="android:windowExitAnimation">@anim/push_left_out</item>


    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/white</item>
    </style>

</resources>
